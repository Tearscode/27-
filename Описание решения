Нам не просто так дали данные в виде текстового файла и в виде Excel файла. Воспользуемся этой подсказкой
Выделим столбцы A и B в экселевском файле и создадим диаграмму. Выберем вставка -> диаграмма -> точечная. На картинке 1 четко видно два класстера (рассматриваем файл А).
Далее мы можем с помощью перебора найти центры каждого класстера. 

Заведем два списка a1 и a2. В первый список будем класть точки из первого кластера, во второй список из второго кластера.
Визуально видно в диаграмме Excel, что точки из первого кластера по оси X меньше единицы, а из второго больше.
Перебором пробуем для каждой точки из кластера найти сумму расстояний до всех точек этого кластера. Где получается минимальная длина, запоминаем координаты x и y этой точки.

Вот и все решение, а для файла B решение будет аналогичным.
